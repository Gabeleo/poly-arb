global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yaml

scrape_configs:
  - job_name: polyarb
    static_configs:
      - targets: ["daemon:8080"]
    metrics_path: /metrics
    scrape_interval: 10s
